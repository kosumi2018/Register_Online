@model IEnumerable<DataModels.StudentAcc>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StuAcc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StuAcc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentAccId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentAccId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentAccId })
        </td>
    </tr>
}

</table>
